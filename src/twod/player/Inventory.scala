package twod.player

class Inventory extends Container {
  
  protected var items: List[Item] = List()
  
  override def add(i: Item) = {
    
  }
  
  override def add(i: Item, index: Int) = {
    
  }
  
  override def get(index: Int) = {
    
  }
  
  override def remove() = {
    
  }
  
  override def remove(index: Int) = {
    
  }
  
  override def size(): Int = {
    1
  }
}